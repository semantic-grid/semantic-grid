{
  "name": "db-meta",
  "scripts": {
    "dev": "uv run python -m db_meta_server.run --port 3030",
    "build": "uv build",
    "lint": "uv run ruff check .",
    "test": "uv run pytest -q",
    "setup": "uv sync --frozen --all-extras"
  }
}